kiwi-assistant-template-preview-0.0.1.vsix
    安装该 VS Code 扩展后，鼠标悬停在 find_image / find_images 的 subpath 参数上即可预览模板图片并查看尺寸。
    After installing this VS Code extension, hovering over the subpath parameter of find_image or find_images will display a preview of the template image along with its dimensions.